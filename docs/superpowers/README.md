# Frozen archive (as of 2026-07-25)

This tree holds every spec (`specs/`) and implementation plan (`plans/`) written before
2026-07-25, when the project switched its spec/plan workflow to `/to-spec` → `/to-tickets` →
`/implement`.

- **Do not add new files here.** New specs go in `docs/specs/`, new plans in `docs/plans/` —
  see `docs/agents/issue-tracker.md`.
- **Do not move or edit the existing files.** `BUILD_STATUS.md`, `ISSUES.md`, and the domain
  docs link into this tree extensively; it stays exactly where it is so those links keep
  resolving.
