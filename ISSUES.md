# Issues & Bugs

Log every bug or unexpected behaviour here — while building, during manual testing, or reported from the pilot. Don't skip logging something just because the fix was quick; the record matters more than the fix itself.

**Format per entry:** `ID`, one-line summary, where it was found (story #), severity, status.

---

## Open

| ID | Summary | Found in | Severity | Status |
|---|---|---|---|---|
| ISSUE-6 | Docker entrypoint re-runs the seed on every boot, re-applying the `.env.docker` staff/admin passwords each restart — a password rotated directly in the DB is silently reverted on the next `docker compose up` | Dockerization, background commit security review of `cdb6e6b` | Minor | Won't fix now — intended declarative-credentials behavior for the single-tenant MVP; the seed is idempotent so it's safe, and the footgun is documented in `docker/entrypoint.sh`. Revisit if per-user accounts or DB-side password management are added. |
| ISSUE-7 | App/DB secrets (`AUTH_SECRET`, `SEED_*`, `POSTGRES_PASSWORD`) are passed to containers as plaintext env vars via `env_file: .env.docker` | Dockerization, background commit security review of `7e548f3` | Minor | Won't fix now — standard Compose mechanism for a local "deploy-later" stack; `.env.docker` is gitignored and uncommitted (verified), so nothing leaks to the repo. **Harden before any real deployment**: move to Docker secrets / a secrets manager and rotate `AUTH_SECRET`. |
| ISSUE-11 | `vercel-build`'s seed step (`prisma/seed.ts`) is the only code path that ever writes `Credential.passwordHash` (no per-user accounts, ADR-003) — it re-runs on every Vercel deploy, including preview builds, which share the production Neon DB. A password rotated directly in the DB (e.g. emergency incident response) is silently reverted on the next deploy, and any PR can trigger that revert via its preview build | Production deployment plan (`docs/superpowers/plans/2026-07-08-production-deployment.md`), background commit security review of the `vercel-build` script | Minor | Won't fix now — extends `ISSUE-6`'s already-accepted precedent to production; there is no other password-rotation mechanism in this system, so editing `SEED_ADMIN_PASSWORD`/`SEED_STAFF_PASSWORD` in Vercel and redeploying **is** the intended rotation path, not a bypass of one. Direct DB-side credential edits are not a supported operation and should not be attempted. **Both the Production and Preview environment scopes must be updated** (`SEED_STAFF_PASSWORD`/`SEED_ADMIN_PASSWORD`/`AUTH_SECRET`/`DATABASE_URL` are all set for both scopes and both seed the same shared production database, per the design spec's Decision 4) — updating only one scope leaves the rotation incomplete, since the next build under the stale scope reseeds the shared DB with the old value. Revisit if per-user accounts or a real password-change flow are ever added. |
| ISSUE-12 | Production `SEED_ADMIN_PASSWORD`/`SEED_STAFF_PASSWORD` were set to weak, guessable placeholder-style values (a short, dictionary-word/placeholder-style password) rather than password-manager-generated ones, and `/api/auth/login` has no rate limiting or lockout — an unauthenticated attacker can script unlimited password guesses against a small, guessable search space | First production deploy, manual smoke test of `docs/superpowers/plans/2026-07-08-production-deployment.md` Task 7 | Major | Open — accepted as-is for now by the project owner ("fine for now") ahead of a confirmed pilot. Fix: rotate `SEED_ADMIN_PASSWORD`/`SEED_STAFF_PASSWORD` to strong random values in **both the Production and Preview environment scopes** in Vercel (rotation mechanism per `ISSUE-11`) and redeploy — updating only one scope does not fully rotate the credential, since the other scope will reseed the shared DB with the stale value on its next build — before any real pilot goes live; consider basic rate limiting on `/api/auth/login` if per-employee accounts aren't built first. |
| ISSUE-20 | `npx eslint .` reports `react-hooks/set-state-in-effect` errors — currently 2, in `app/order/Cart.tsx:74` (cart-restore effect) and `app/order/OrderHeaderTitle.tsx:12` — plus pre-existing `react-hooks/exhaustive-deps` warnings. **Down from 3 errors**: the third (`react-hooks/immutability`, `staggerIndex += 1` during render, formerly `Cart.tsx:244`) was **removed** by the menu-management mirror redesign (Story 21a), which rewrote `Cart.tsx` to render through the shared `MenuGroups` component and dropped the mutable cross-category stagger counter. Line numbers shifted with that rewrite (`Cart.tsx:83`→`:74`) | Task 9 of the menu-categories plan; line-numbers/count refreshed during Story 21a Task 11 | Minor | Open — lint-only, no user-facing or data-integrity impact. Pre-existing, not introduced by either feature. `OrderHeaderTitle.tsx`'s error reproduces earlier prescribed verbatim code; `Cart.tsx:74` is the sessionStorage cart-restore-on-mount effect. Needs a follow-up to fix the 2 remaining call sites or add justified eslint-disables and get back to a clean `npx eslint .`. |
| ISSUE-21 | `prisma/seed.ts`'s admin-credential create-once is a non-atomic check-then-act (`findFirst({ role: 'admin' })` then update-by-id-or-create). The branch-model migration (Task 2) dropped `Credential.role`'s `@unique` index — needed because `role` can no longer be unique once each branch has its own staff credential — so the old race-safe `upsert({ where: { role } })` can't be preserved for the admin row. Under this project's documented concurrent-deploy-on-shared-DB conditions, two overlapping seed runs against an **unseeded** DB could each take the create branch and insert two admin rows | Task 10 (`prisma/seed.ts` rewrite) reviewer follow-up on the dropped-unique-constraint race | Minor | Open — practical risk is near-nil: the shared production/dev DB is already seeded (an admin row exists), so every real run takes the update path, not create; the window only exists on a truly empty DB with two simultaneous seeds. Unlike the Main branch + its staff credential (now atomic upserts keyed on the fixed branch id / `@unique` branchId respectively), admin has no safe atomic key — `role` isn't unique, `branchId` is null (can't upsert on a null unique field), and its id is random in the existing DB (a fixed-id upsert would insert a duplicate, not match). Proper fix: add a partial unique index (`CREATE UNIQUE INDEX ... ON "Credential"("role") WHERE "role" = 'admin'`) in a future migration, then switch this to a real `upsert` — a schema migration, out of Task 10's scope. |
| ISSUE-23 | `branchService.createBranch` performs three sequential, non-transactional Prisma writes (`branch.create` → `orderingPoint.create` → `credential.create`). A failure between any two leaves an orphaned branch — visible in `listBranches()`/the branch selector but with no Counter ordering point and/or no staff credential, so staff can never log into it | Plan 2 (multi-branch admin UX), Task 1 task review + final whole-branch review of `docs/superpowers/plans/2026-07-11-multi-branch-admin-ux.md` | The task brief's prescribed implementation (which the implementer followed verbatim) never wrapped the three calls in `prisma.$transaction([...])`; flagged as plan-mandated rather than an implementer defect in both reviews | Open — compounds with this same plan's deliberate no-branch-deletion decision (per its design spec), since an orphaned branch created this way has no in-app cleanup path either. Fix: wrap the three creates in `prisma.$transaction([...])` in a follow-up — a few lines, no schema change needed. |

| ISSUE-26 | `Modal` registers its Escape handler on `document` with no stacking discipline, so pressing Escape over a stacked dialog closes **every** open modal at once, not just the topmost. Repro: dashboard → open an order → `×` on a line → "Remove item?" appears → press Escape → both the confirm dialog *and* the order detail modal close. Same for "Cancel this order?" | Grilling session for the counter add-item redesign, 2026-07-23; pre-existing on `dev` (predates the receipt-printing work) | Minor | Open — cosmetic-ish today (Escape is a dismiss gesture, so the worst case is losing your place in an order, not a wrong write), but it becomes load-bearing if any further layer is stacked on `OrderDetailModal`. Fix: give `Modal` a shared stack (module-level counter or context) and have the keydown handler no-op unless the instance is topmost; alternatively bind on the dialog element with focus containment instead of `document`. `app/components/Modal.tsx:20-26`; stacking is `order-detail-modal__backdrop` z-30 under `confirm-dialog__backdrop` z-40 |

Severity: `Critical` (blocks core loop / data integrity) · `Major` (breaks a story's AC) · `Minor` (edge case, cosmetic)

---

## Resolved

| ID | Summary | Found in | Root cause | Fix / commit |
|---|---|---|---|---|
| ISSUE-1 | API error codes had a redundant `_ERROR` suffix (`INVALID_CREDENTIAL_ERROR` instead of `INVALID_CREDENTIAL`), contradicting `05-api-conventions.md`'s own example | Story 1, task review | `codeFor()` in `lib/handleApiError.ts` converted the exception class name to SCREAMING_SNAKE without stripping the trailing "Error" | Stripped the suffix in `codeFor()`; commit `f08afba` |
| ISSUE-2 | Design spec and a test fixture still said `VALIDATION_ERROR` after ISSUE-1's fix changed the real runtime output to `VALIDATION` | Story 1, final whole-branch review | ISSUE-1's fix wasn't propagated to the design doc's error table or `lib/apiClient.test.ts`'s hand-written mock fixture | Updated both to `VALIDATION`; commit `18adc0b` |
| ISSUE-3 | `verifySession` didn't validate the JWT's `role` claim was actually `'staff'`/`'admin'` — an unrecognized value would silently pass any `requireRole()` check instead of being rejected | Story 1, final whole-branch review | `ROLE_RANK[unknownRole]` evaluates to `undefined`, and `undefined < ROLE_RANK[minRole]` is `false`, so the guard's rejection condition never triggered | Added an `isRole()` type guard so `verifySession` returns `null` for any non-staff/admin role claim (fail closed); commit `18adc0b` |
| ISSUE-4 | Two Postgres instances (a Docker container + a pre-existing native Windows service) both listened on port 5432; `localhost` resolved ambiguously between them, causing "migration ran but seed data isn't there" confusion | Story 1, Task 2 build | Docker's port mapping bound successfully alongside an already-running native Postgres service with no conflict error; IPv6/IPv4 resolution order picked one nondeterministically | Removed the Docker container, standardized on the native Postgres service, pinned `DATABASE_URL` to `127.0.0.1` instead of `localhost` in `.env.local` | 
| ISSUE-8 | In the **production build**, every API error's `error` code came back empty (`{"error":"","message":"..."}`) instead of a short code like `CONFLICT`/`NOT_FOUND`/`VALIDATION`, violating the `05-api-conventions.md` error envelope | Story 6, live Docker smoke test (`DELETE /api/orders/:id/items/:id` on the last item; also reproduced on `POST /api/orders` empty-cart 400 and a 404) | `codeFor()` in `lib/handleApiError.ts` derived the code from `error.name` (= `this.constructor.name` in `lib/errors.ts`); Next.js SWC production minification mangles class names to empty/garbage. HTTP statuses stayed correct because `statusFor()` already used `instanceof` (survives minification) — only the code string broke. Invisible to unit tests, which run unminified. | Rewrote `codeFor()` to use the same `instanceof` chain as `statusFor()` instead of `error.name`; added a regression test that mangles `error.name` before calling `handleApiError` to prove the code no longer depends on it. Re-verified live against the rebuilt Docker image: `VALIDATION`/`NOT_FOUND`/`CONFLICT` all correct. Commit `a5d0020`. |
| ISSUE-10 | `/order/[id]` showed "Order #N confirmed" as the ticket heading while the order was still `Pending` (editable) — contradicting the status note directly below it | User report while verifying Story 10a's dashboard tabs/tile work | `OrderTicket.tsx`'s `heading` prop was copy-pasted verbatim from the actual-Confirmed branch in `page.tsx` without updating the copy for the Pending state | Heading changed to `Order #{N}` (no status claim), matching the Cancelled branch's plain heading; commit b0e089b |
| ISSUE-14 | The global `StaffBar` (role/Dashboard/admin-nav/Log out toolbar, Story 14) was completely invisible immediately after logging in — only a hard page reload made it appear | Story 14, Docker/Playwright smoke test of the global toolbar | `app/login/page.tsx` navigated with `router.push('/dashboard')` and no `router.refresh()`. The root layout (`app/layout.tsx`) reads the session via `peekSession()` to decide whether to render `StaffBar`; since that layout is a shared ancestor of both `/login` and `/dashboard`, Next's App Router reused its prior render (session `null`, from when the user was still on `/login`) across the client-side `push()` instead of refetching it | Added `router.refresh()` immediately after `router.push('/dashboard')` in `app/login/page.tsx`, forcing the whole route tree — including the root layout — to refetch server data on this transition; commit `6698744` |
| ISSUE-5 | Staff Dashboard's "Menu Management" nav link points to `/admin/menu`, but the actual route is `/admin/menu-items` — clicking it 404s | Story 7 planning, noticed while wiring `PendingOrdersDashboard` into `app/dashboard/page.tsx` | Hardcoded `href` never matched the real route created in a later story | Changed `href` to `/admin/menu-items` in `app/dashboard/page.tsx`; fixed alongside Stories 12-13's dashboard work |
| ISSUE-18 | `StaffBar`'s "Menu Management" nav link was gated `role === 'admin'`, so a `staff` session had no way to navigate to `/admin/menu-items` — even though Story 17 gave staff real authority there (toggling a menu item's sold-out status via `requireRole('staff')`) | Story 17, manual smoke test of `feature/business-hours-and-staff-sold-out` | Pre-existing gate (`app/components/StaffBar.tsx`, predates Story 17 — confirmed via `git show` on the branch's base commit) was never revisited when Story 17 extended the underlying page's authority to staff | Dropped the `role === 'admin'` condition from `showMenuManagementLink` so it now matches the ungated `showDashboardLink` pattern (`pathname !== '/admin/menu-items'`); `showTableSetupLink`/`showSettingsLink` remain admin-only. Updated `StaffBar.test.tsx` to cover staff seeing the link (with correct `href`) and it hiding while already on that page |
| ISSUE-15 | Visiting a table's order URL directly with `https://` "doesn't work" (blank/error page); `http://` does | Manual verification of the Story 15/16 branch, 2026-07-09 | Local-Docker-only artifact, **not** a production defect (confirmed by project owner 2026-07-09: prod/dev environments work correctly). `app/admin/tables/page.tsx:13` guesses the protocol from `process.env.NODE_ENV === 'production' ? 'https' : 'http'` rather than reading the actual request scheme. On Vercel this aligns (build is `production` **and** TLS is served) so the QR/order URL is correct; on `npm run dev` it aligns the other way (`development` → `http`). It only misfires for a *local production build* — `docker compose` (`3001:3000`) runs a `production` build served over plain HTTP, so it prints an unreachable `https://localhost:3001/…` URL. | Closed — no code change (project owner opted to just close, since prod/dev are correct). Known cosmetic gap: local-docker QR/URL text shows `https://localhost:3001`; use `http://localhost:3001` directly when testing locally. If it's ever worth fixing, derive the protocol from the `x-forwarded-proto` header (fallback `http`) instead of `NODE_ENV`, which is correct in every environment. |
| ISSUE-9 | Re-adding a cart item during its ~200ms removal fade (tap `-` on qty 1, then re-tap the menu item within that window) silently dropped the re-add: the pending removal timer fired unconditionally and decremented once more, leaving the line at one fewer than the customer just set | Cart/order UX polish pass, final whole-branch review of `docs/superpowers/plans/2026-07-07-cart-ux-polish.md` | `addItem` (`app/order/Cart.tsx`) had no awareness of `removingLineIds`/`removingLineTimersRef`, so a re-add mid-fade bumped quantity, then the still-pending removal timer decremented it again on top of that | `addItem` now looks up `removingLineTimersRef` for the item id; if a removal is pending it clears the timeout, removes the id from `removingLineTimersRef`/`removingLineIds`, and only then applies the quantity bump — mirrors how `showToast`/`openReview` already abort their own pending exit on re-entry. Added a fake-timer regression test (`Cart.test.tsx`, "cancels the pending removal..."). Commit `b0ef3b4` |
| ISSUE-16 | `tsc --noEmit` failed on `app/admin/menu-items/MenuItemRow.test.tsx` — `vi.mocked(apiClient.del).mockResolvedValue({})` doesn't type-check because `apiClient.del` returns `Promise<void>`, not `Promise<{}>` | Task 1 of the business-hours/staff-sold-out feature (`docs/superpowers/plans/2026-07-09-business-hours-and-staff-sold-out.md`), task-review of the `VenueSettings` migration commit | The Archive test's mock return value (`{}`) didn't match `apiClient.del`'s actual `Promise<void>` signature; harmless at runtime since Vitest doesn't enforce mock return types, but broke a clean `tsc --noEmit` | Changed to `mockResolvedValue(undefined)`. Commit `b0ef3b4` |
| ISSUE-17 | `eslint` failed with 9 `react-hooks/error-boundaries` ("Avoid constructing JSX within try/catch") errors in `app/order/page.tsx` | Pre-existing on `dev`; final whole-branch review of the business-hours/staff-sold-out feature (`docs/superpowers/plans/2026-07-09-business-hours-and-staff-sold-out.md`) added 2 more instances of the identical pattern (Task 7's closed-venue branch) | `OrderPage`'s three `return (...)` JSX blocks all lived inside its outer `try`; the rule flags this because React error boundaries can't catch render errors from JSX constructed (not just rendered) inside a `try` | Narrowed the `try` to wrap only `getTableOrThrow` (the one call that can throw `NotFoundError`) with no JSX inside it; `getVenueSettings`/`listMenuItems` and all JSX now live outside the `try`/`catch`. Commit `b0ef3b4` |
| ISSUE-19 | Dead `.ticket__remove` CSS rule (and its `:hover`/`:active`/`:focus-visible`/`:disabled`/reduced-motion variants) left in `app/globals.css` after removing the customer-facing remove-item button | Story 18 (customer order editing restricted to cancel-only), task review of `app/order/[id]/OrderTicket.tsx`'s remove-item removal | The component using class `ticket__remove` was deleted, but the CSS wasn't removed in the same commit since `app/globals.css` wasn't in that task's file list | Deleted the dead rule block from `app/globals.css` as a same-branch follow-up |
| ISSUE-13 | Anonymous callers could add/remove/adjust-quantity items on any Pending order whose opaque id they possessed | Staff/admin order-edits feature, final whole-branch review of `docs/superpowers/plans/2026-07-09-staff-admin-order-edits.md` | The three items routes read the caller's role via non-redirecting `peekSession()` but never required one — any role (including none) was accepted | Story 18 restricted this capability to staff/admin only rather than hardening the anonymous path: `POST`/`PATCH`/`DELETE` on `/api/orders/:id/items[/:itemId]` now call `requireApiRole('staff')`, returning `403` for anyone else; the customer-facing remove-item button was also removed from `/order/[id]` as it's no longer backed by a reachable capability |
| ISSUE-22 | `branchService.setBranchPassword`'s INV-15 collision check silently excluded the admin credential from the scan on password rotation, letting a branch's staff password be rotated to be identical to the admin's actual password with no conflict raised — a live-reproduced login-misrouting hazard (unordered `findMany`+`bcrypt.compare` in `login()` means whichever credential matches first "wins" the shared password) | Plan 2 (multi-branch admin UX), final whole-branch review of `docs/superpowers/plans/2026-07-11-multi-branch-admin-ux.md` | `assertPasswordAvailable`'s exclusion query used `{ where: { branchId: { not: excludeBranchId } } }`; `Credential.branchId` is nullable (admin's row has `branchId: null`), and SQL three-valued logic means `NULL <> 'some-id'` evaluates to `NULL`, not `TRUE` — so Prisma's `not` filter silently drops NULL rows, excluding the admin credential from every rotation-time collision scan. Confirmed empirically: rotated a branch's password to the real admin password via `PATCH /api/branches/:id` and got `200` instead of `409` | Changed the query to `{ where: { OR: [{ branchId: { not: excludeBranchId } }, { branchId: null } ] } }` so NULL-branchId (admin) rows are correctly retained in the scan. Added a regression test asserting the corrected WHERE-clause shape (the existing mock-based tests couldn't otherwise distinguish old vs. new query shape, since the mock doesn't implement real Prisma filtering) plus a collision test against an admin credential. Re-verified live via Docker Compose: the same rotation attempt now returns `409 Conflict`. Commit `7b8de6e` |
| ISSUE-24 | Admin's staff-assisted "+ New order" flow ignored the branch selected in the header and always placed the order in the first branch (by name). Repro: as admin, switch the header branch picker to a non-first branch → "+ New order" → pick a table → submit → order recorded under the first branch. Staff (branch-scoped login) unaffected | User report, manual multi-branch testing 2026-07-11 | The 2026-07-11 admin-UI branch-context redesign wired `?branch=` into `/dashboard`, `/admin/menu-items`, `/admin/tables` (`StaffBar.BRANCH_AWARE_PATHS`) but never included `/order/new`. The dashboard's "+ New order" `<Link>` was hardcoded to `/order/new` (dropping the branch), and `app/order/new/page.tsx` never read `searchParams` — it called `resolveBranchId(session)` with no `requestedBranchId`, which for an admin (no `session.branchId`) falls through to `listBranches()[0]`. The listed tables were therefore always the first branch's, and `createOrder` faithfully derives `branchId` from the chosen table (`INV-13`), landing the order there | Threaded the selection end-to-end: the dashboard "+ New order" link now carries `?branch=<selected>` for admins (`app/dashboard/page.tsx`), and `/order/new` reads `searchParams.branch`, resolving staff → their `session.branchId`, admin+specific-branch → that branch's tables (with the branch name shown in the header for clarity), and admin+"all"/missing/stale-id → an explicit **branch-chooser** step rather than silently defaulting to one branch. 10 new/updated tests across `app/order/new/page.test.tsx` and `app/dashboard/page.test.tsx`; full suite 564 green, `tsc`/`eslint` clean. Gotcha logged in `BUILD_STATUS.md`. Confirmed end-to-end via a live Docker Compose multi-branch smoke test (38/38 checks across customer/staff/admin: an admin-selected non-first-branch order records under that branch). Shipped to dev/preprod/main in commit `1d070d0`. |
| ISSUE-25 | `categoryService.moveCategory` read the category list then wrote the sortOrder swap in a separate transaction — a non-atomic read-then-write two concurrent admins could interleave and corrupt | Menu categories feature, final whole-branch review | Two-step read-then-transactional-write with no read-write atomicity guard | Resolved by the menu-management mirror redesign: `moveCategory` and its `/api/categories/:id/move` route were removed entirely and replaced by `reorderCategories` / `PATCH /api/categories/reorder`, which validates the full id set and rewrites all sortOrders in a single `prisma.$transaction` — no separate read-then-write remains. |

---

## Template for a new entry

```
### ISSUE-<N>: <one-line summary>
- **Found in.** Story <#> / manual testing / pilot report
- **Severity.** Critical / Major / Minor
- **Repro.** <steps or conditions that trigger it>
- **Expected vs actual.** <what should happen> vs <what happens>
- **Root cause.** <once known>
- **Fix.** <what changed, or link to the commit>
- **Status.** Open / Investigating / Fixed / Won't fix (and why)
```
